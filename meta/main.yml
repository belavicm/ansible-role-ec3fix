galaxy_info:
  author: mbelavic@srce.hr
  description: Install ec3-cloud-users cluster
  company:  Srce
  license: Apache
  min_ansible_version: 2.0
  platforms:
  - name: RedHat
    versions:
    - trusty
  galaxy_tags:
    - ec3-cloud-users
#dependencies:
#  - { role: 'grycap.ntp', ntp_config_server: [ntp.upv.es, ntp.uv.es] }
#  - { role: 'grycap.ssh', ssh_type_of_node: "{{ sge_type_of_node }}", ssh_user: "{{ sge_ssh_user }}" }
